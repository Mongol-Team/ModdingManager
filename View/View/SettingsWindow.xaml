<Window x:Class="View.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки"
        Width="600" Height="250"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#000000">
    <Grid Margin="24" Background="#000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Настройка путей" 
                   Style="{StaticResource TextPageTitle}" 
                   Margin="0,0,0,24"/>

        <Label Grid.Row="1" Content="Директория игры" 
               Style="{StaticResource TextLabel}" 
               Margin="0,0,0,8"/>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="GameDirBox" Grid.Column="0" Margin="0,0,12,0"
                     Style="{StaticResource TextBoxDark}"/>
            <Button x:Name="BrowseGameButton" Grid.Column="1" 
                    Content="Обзор..." Width="100"
                    Style="{StaticResource ButtonDark}" Click="BrowseGameButton_Click"/>
        </Grid>

        <Label Grid.Row="3" Content="Директория мода" 
               Style="{StaticResource TextLabel}" 
               Margin="0,0,0,8"/>

        <Grid Grid.Row="4" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ModDirBox" Grid.Column="0" Margin="0,0,12,0"
                     Style="{StaticResource TextBoxDark}"/>
            <Button x:Name="BrowseModButton" Grid.Column="1" 
                    Content="Обзор..." Width="100"
                    Style="{StaticResource ButtonDark}" Click="BrowseModButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="Отмена" Width="120" 
                    Style="{StaticResource ButtonDark}" Margin="0,0,12,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="Сохранить" Width="120" 
                    Style="{StaticResource ButtonLight}"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

