<base:BaseWindow x:Class="View.GameDirectorySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Controls;assembly=Controls"
        xmlns:base="clr-namespace:View"
        xmlns:loc="clr-namespace:Controls.Utils;assembly=Controls"
        Title="{loc:Localize Key=Window.GameDirectorySettings}"
        Width="500" Height="150"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Style="{StaticResource WindowDark}">
    <Grid Background="{StaticResource BackgroundApp}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <controls:WindowTitleBar Grid.Row="0"/>
        
        <Grid Grid.Row="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label x:Name="GameDirectoryLabel" Grid.Row="0" Content="{loc:Localize Key=Label.GameDirectory}" 
                   Style="{StaticResource TextLabel}" 
                   Margin="0,0,0,8"/>

            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="GameDirBox" Grid.Column="0" Margin="0,0,12,0"
                         Style="{StaticResource TextBoxDark}"/>
                <Button x:Name="BrowseGameButton" Grid.Column="1" 
                        Content="{loc:Localize Key=Button.Browse}" Width="100"
                        Style="{StaticResource ButtonDark}" Click="BrowseGameButton_Click"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" 
                        HorizontalAlignment="Right" Margin="0,0,0,0">
                <Button x:Name="CancelButton" Content="{loc:Localize Key=Button.Cancel}" Width="120" 
                        Style="{StaticResource ButtonDark}" Margin="0,0,12,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="SaveButton" Content="{loc:Localize Key=Button.Save}" Width="120" 
                        Style="{StaticResource ButtonLight}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</base:BaseWindow>

